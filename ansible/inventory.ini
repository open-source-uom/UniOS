# ansible/inventory.ini
# Targets the running QEMU VM via SSH on localhost:2222.
# The 03_run_ansible.sh script generates a temp inventory at runtime
# with credentials — this file is the static reference/template.

[custom_distro]
localhost ansible_port=2222 ansible_user=kubuntu ansible_ssh_common_args='-o StrictHostKeyChecking=no'
