# ansible/inventory.ini
# This file is used for direct ansible-playbook runs.
# The 03_run_ansible.sh script generates a temp inventory pointing
# at the actual chroot path at runtime - this file is the template.

[custom_distro]
/tmp/chroot_dir ansible_connection=chroot
